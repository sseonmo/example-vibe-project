# 2026-06-06 작업일지

## 14:24 작업일지 작성 유도 Stop 훅 추가

- `.claude/hooks/worklog-stop.sh` 신규 작성: 코드 변경이 있는 턴이 끝날 때 오늘자 작업일지가 없거나 오래됐으면 block하고 3~5줄 작성 지시
- `.claude/settings.json`에 Stop 훅으로 등록 (구현 작업마다 작업 내역이 기록되도록 강제하기 위함)
- TDD로 검증: 시나리오 5건(루프 방지·미발동 조건·발동·재발동) Red→Green 통과, 실제 세션에서 라이브 발동도 확인

## 14:27 Prettier 자동 포맷 PostToolUse 훅 추가

- `.claude/hooks/prettier-format.sh` 신규 작성: Edit/Write 직후 수정된 파일을 `npx prettier --write --ignore-unknown`으로 자동 포맷
- `.claude/settings.json`에 PostToolUse(Write|Edit) 훅으로 등록 — 수동 `npm run format` 실행과 포맷 불일치 커밋을 없애기 위함
- 검증: 실제 세션에서 파일 수정 시 훅 동작 확인 (수정 시각: 사용자 직접 작성)

## 16:15 실습 작업 선정 — IDEAS #1~#3을 TODO.md로 이동

- `TODO.md` 신규 생성: IDEAS.md의 #1(호버 툴팁)·#2(다크/라이트 토글)·#3(feedback API 완성)을 체크리스트로 이동
- `IDEAS.md`에서 해당 항목 제거 후 이동 안내 문구만 남김 (#4~#7은 그대로)
- 검증: 두 파일 내용 대조로 누락 없음 확인, prettier 훅 자동 포맷 적용됨

## 16:20 IDEAS.md에 #1~#3 항목 복구

- `IDEAS.md`의 "TODO.md로 이동했습니다" 안내 문구를 제거하고 #1(호버 툴팁)·#2(다크/라이트 토글)·#3(feedback API 완성) 원문을 복구
- 사용자 요청에 따라 IDEAS.md에는 전체 후보 목록을 유지하고, `TODO.md`는 수정 없이 그대로 둠
- 복구 형식은 현재 파일의 Prettier 스타일(제목·파일 줄 사이 빈 줄)에 맞춰 작성
- 검증: git diff와 TODO.md 원문을 대조해 세 항목 내용이 누락 없이 동일함을 확인

## 16:29 TODO #1 계획 문서 작성 (구현은 보류)

- `docs/plans/001-agent-loop-hover-tooltip.md` 신규 생성: `/agent-loop` 노드 호버 툴팁(TDD·a11y·ESC 닫힘) 구현 계획
- 계획 문서 컨벤션 도입: `docs/plans/NNN-목적-요약.md` (3자리 순번 + 목적 kebab-case)
- 사용자 피드백 반영: 계획 승인 ≠ 구현 허가 — 임의로 먼저 만들었던 `tests/loop-diagram.test.tsx`는 삭제, 구현은 별도 지시 시 착수
- 검증: 코드 변경 없음(문서만), 삭제 후 working tree에 계획 문서만 남은 것 확인

## 16:30 CLAUDE.md 절대 규칙 보강 — 계획 승인 ≠ 구현 허가

- 절대 규칙 2번 신설: "계획 승인은 구현 허가가 아니다" — 구현 착수 전 별도 질문·명시적 답 필수
- 규칙 1번에 계획 문서 컨벤션(`docs/plans/NNN-목적-요약.md`) 명시
- 계획 승인 직후 임의로 구현을 시작했던 실수의 재발 방지를 위한 사용자 지시 반영
- 검증: prettier 훅이 규칙 번호를 1~5로 자동 재정렬한 것까지 파일 재확인

## 16:38 plan-doc 스킬 추가 — 계획 문서 생성 절차 자동화

- `.claude/skills/plan-doc/SKILL.md` 신규: 순번 계산 → kebab-case 이름 부여 → 템플릿 생성 → 계획 승인 → 구현 여부 별도 확인(절대 규칙 2 내장) → 검증 스킬 연계 안내
- 계획 문서 `docs/plans/002-plan-doc-skill.md` 작성·승인 후 구현 (첫 프로젝트 레벨 스킬)
- 검증: 순번 계산 명령 실행으로 다음 번호 003 확인, frontmatter 규격 확인. 실제 `/plan-doc` 호출은 새 세션에서 확인 필요 (미검증)

## 16:40 plan-doc 스킬 적용 범위 수정 — 계획 문서는 코드 구현 작업에만

- 사용자 정정 반영: 스킬·훅·설정·문서 등 비구현 작업에는 계획 문서를 만들지 않음
- `.claude/skills/plan-doc/SKILL.md`에 "적용 범위" 섹션 추가, CLAUDE.md 절대 규칙 1에도 동일 내용 명시
- 비구현 작업(스킬 생성)인데 만들었던 `docs/plans/002-plan-doc-skill.md` 삭제 — plans에는 001만 남음
- 검증: docs/plans/ 목록 확인, 스킬 목록 갱신으로 plan-doc이 현재 세션에 로드된 것 확인

## 16:51 cust_review 스킬로 001 계획 문서 검증·보강

- `/cust_review`로 sub-agent 위임 검증 (h1→h1→h2→h3 × 3라운드): 이슈 18→4→1건으로 수렴, 최종 ✅
- `docs/plans/001-agent-loop-hover-tooltip.md` 보강: ESC를 document keydown으로 변경(호버 시 미동작 결함), fireEvent.mouseEnter→mouseOver 정정, role="button" 안티패턴 제거, svg role="group" 변경, 툴팁 % 좌표 기준 정렬, 테스트 5건→8건, 한계/스코프 외 섹션 신설
- 코드 변경 없음 (계획 문서만 수정), 구현은 여전히 보류 상태
- 검증: sub-agent가 실제 코드(LoopDiagram.tsx 등)와 대조하며 3라운드 재검증, 마지막 라운드 잔여 이슈 0건 확인

## 16:55 개발 서버 실행

- 사용자 요청으로 `npm run dev`를 백그라운드로 실행 — 코드 변경 없음
- 포트 3000이 이미 사용 중이라 Next.js가 자동으로 3001로 대체 (http://localhost:3001)
- 검증: 서버 로그에서 "Ready in 945ms" 확인

## 16:53 plan-doc 스킬에 셀프 체크리스트 4항목 추가

- `.claude/skills/plan-doc/SKILL.md` 템플릿에 "셀프 체크리스트" 섹션 신설: 범위 적절 / 기존 패턴 준수 / 테스트 포함 / 리스크 명시
- 절차 4단계를 "체크리스트 점검 후 승인 요청"으로 변경 — 4항목 모두 ✅여야 승인 요청 가능
- 계획 문서 품질 기준을 매번 자동 점검하기 위한 사용자 요청 반영
- 검증: 비구현(스킬 문서) 작업이라 계획 문서 없이 진행, 수정 내용 본문 확인

## 17:09 개발 서버 재실행 및 잔여 worktree 정리

- 사용자 요청으로 기존 백그라운드 dev 서버 종료 후 `npm run dev` 재실행 (포트 3000 점유로 3001 사용)
- verify 훅 실패 원인 발견: `/cust_review` sub-agent가 남긴 worktree(`.claude/worktrees/iterative-sprouting-bear`)의 미구현 툴팁 테스트 7건을 메인 vitest가 수집
- `git worktree remove --force`로 잔여 worktree와 `worktree-iterative-sprouting-bear` 브랜치 삭제 — 본 저장소 코드 변경 없음
- 검증: `make verify` 재실행으로 typecheck·lint·test 5건 전부 통과 확인

## 17:21 `/agent-loop` 단계별 호버 툴팁 구현 (docs/plans/001, TODO #1)

- `components/LoopDiagram.tsx`를 클라이언트 컴포넌트로 전환하고 6개 노드에 호버/포커스/클릭 시 설명 툴팁(`role="tooltip"`) 추가 — `STEPS`에 `description` 필드 신설, `role="img"` → `role="group"` 전환, document 레벨 ESC·바깥 클릭 닫기(WCAG 1.4.13), `focus-visible` 포커스 링(WCAG 2.4.7), 노드 위치별 펼침 방향 조정
- TDD로 진행: `tests/loop-diagram.test.tsx` 8건 작성 → Red(7건 실패) 확인 → 구현 → Green. `TODO.md` #1 체크
- 주의: 최초 구현은 worktree(`iterative-sprouting-bear`)에서 진행했으나 17:09 다른 세션의 worktree 정리(`git worktree remove --force`)로 미커밋 작업이 삭제됨 — 본 저장소에 재작성으로 복원 (17:09 항목의 "미구현 툴팁 테스트"가 바로 이 작업이었음)
- 검증: `npm test` 13건 전체 통과(기존 회귀 없음), `npm run typecheck`·`npm run lint` 통과, dev 서버(포트 3000)가 신규 마크업(`role="group"`, tabindex 노드 6개) 서빙 확인. 호버/ESC/터치 시각 동작은 브라우저 수동 확인 대기

## 17:25 툴팁 배경 불투명 처리 — SVG 중앙 텍스트 비침 수정

- 사용자 브라우저 확인 피드백: `.surface` 그라데이션이 거의 투명(알파 0.04/0.02)이라 툴팁 뒤 SVG 중앙 텍스트("반복 사이클")가 비쳐 글자가 안 읽힘
- 툴팁 div에 `bg-[var(--color-bg-elev)]`(#11111a) 추가로 불투명 배경 적용 — surface의 은은한 그라데이션·테두리는 유지
- 검증: `npm test` 13건·typecheck·lint 통과, dev 서버 200 응답. 시각 확인은 사용자 브라우저 재확인 대기

## 17:28 SVG 좌표 반올림 — hydration 불일치 수정

- 사용자 브라우저에서 hydration 에러 발견: 호 경로 `d` 좌표의 부동소수점 마지막 자리가 서버(Node V8)와 브라우저 엔진 간 미세하게 달라 SSR HTML과 클라이언트 렌더가 불일치 (서버 컴포넌트였을 땐 비교 자체가 없어 잠복해 있다가 "use client" 전환으로 드러남)
- `round2` 헬퍼로 노드 위치·호 시작/끝 좌표를 소수점 2자리로 고정 — 양쪽 엔진에서 동일 문자열 보장
- 검증: `npm test` 13건·typecheck·lint 통과, SSR 출력에서 반올림된 좌표(`233.26 53.73` 등) 확인. 브라우저 콘솔 에러 소멸은 사용자 재확인 대기

## 17:30 독립 리뷰 패턴(워크트리 diff + Explore 서브에이전트) 학습 의도 설명

- 사용자 질문("이걸 하는 의도가 뭘까, 무엇을 배울 수 있지?")에 답변 — 코드·파일 변경 없음
- 핵심 설명: 구현 컨텍스트와 검증 컨텍스트 분리(Generator-Evaluator), 자기 정당화 편향·보상 해킹(헛도는 assertion)·계획-구현 드리프트를 세 체크 항목이 각각 잡는 구조
- 워크트리 격리(병렬 작업 충돌 방지, 머지 전 샌드박스)와 read-only Explore의 안전성도 함께 정리
- 검증: 실행 없음(경로가 플레이스홀더) — 실제 리뷰는 워크트리 경로를 받으면 진행하기로 안내

## 17:40 툴팁 불투명 배경 재수정 — Tailwind 클래스가 무효였던 원인 해결

- 17:25 수정(`bg-[var(--color-bg-elev)]`)이 실제로 미적용이었음: `.surface`는 globals.css의 레이어 밖 CSS라 `@layer utilities`의 Tailwind 유틸리티보다 항상 우선, `background:` 단축 속성이 배경색을 투명으로 되돌림
- 사용자 선택(완전 불투명 배경)에 따라 inline `style.backgroundColor`로 강제 — inline은 모든 시트 규칙보다 우선이므로 확실히 적용, 무효였던 Tailwind 클래스는 제거
- 모든 노드 툴팁에 공통 적용 (6번 한정 아님) — 뒤 SVG 텍스트·콘텐츠가 완전히 가려짐
- 검증: `npm test` 13건·typecheck·lint 통과. 시각 확인은 사용자 브라우저 재확인 대기

## 18:56 TODO #2 계획 문서 작성 및 cust_review 검증 (구현은 보류)

- `docs/plans/002-memory-hierarchy-theme-toggle.md` 신규: `/memory-hierarchy` 다크/라이트 토글 구현 계획 — `data-theme` 속성 + CSS 변수 오버라이드, localStorage 저장, FOUC 방지 인라인 스크립트, TDD 테스트 5건
- `/cust_review`로 sub-agent 위임 검증 (h1→h1→h2→h3 × 3라운드): 이슈 15→3→2건으로 수렴, 최종 ✅ 잔여 0건
- 주요 보강: aria-pressed+동적 레이블 안티패턴 → 고정 레이블로 교체, 라이트 배경에서 `--color-brand-2` WCAG 대비 미달(≈2.5:1) → `#0e7490` 오버라이드 결정, 인라인 스크립트 저장값 검증, CSP·멀티탭 한계 명시
- 코드 변경 없음 (계획 문서만), 구현은 사용자 지시 대기 중
- 검증: sub-agent가 실제 코드(globals.css, layout.tsx 등)·ESLint 룰 소스와 대조하며 3라운드 재검증, 마지막 라운드 3개 스텝 연속 이슈 0건 확인

## 18:59 `/memory-hierarchy` 다크/라이트 토글 구현 (docs/plans/002, TODO #2)

- `components/ThemeToggle.tsx` 신규: `<html data-theme="light">` 토글 + localStorage 저장 (속성 변경 우선, 저장 실패 무시), 고정 `aria-label="라이트 모드"` + `aria-pressed` 패턴
- `app/globals.css`에 `:root[data-theme="light"]` 라이트 팔레트 오버라이드 (brand-2는 WCAG 대비 때문에 #0e7490로 교체), `app/layout.tsx`에 FOUC 방지 인라인 스크립트 + `suppressHydrationWarning`, `/memory-hierarchy` nav에 토글 배치
- TDD: `tests/theme-toggle.test.tsx` 5건 작성 → Red(모듈 없음) 확인 → 구현 → Green. `TODO.md` #2 체크, feature 브랜치에 조기 커밋 2건
- 검증: `npm test` 18건 전체 통과(기존 13건 회귀 없음), typecheck·lint 통과. dev 서버 SSR 출력에서 인라인 스크립트·토글 버튼·컴파일된 라이트 CSS 블록 확인, 홈은 기본 다크 유지. 새로고침 유지·FOUC 부재·라이트 가독성은 사용자 브라우저 확인 대기
